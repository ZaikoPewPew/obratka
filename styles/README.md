# `styles/` — слои стилей

- `tokens.css` — **единый источник дизайн-токенов** (примитивы → семантика → темы). См. `.cursor/rules/design-tokens.mdc`.
- `base.css` — сброс и базовые правила.
- `iframe-shell.css` — оболочка: панель управления, iframe, панель ревью.
