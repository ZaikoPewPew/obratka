---
description: i18n — весь UI через content/locales.json (ru/en, расширяемо)
alwaysApply: true
---

# Локализация (i18n)

Источник правды: `content/locales.json`. API: `src/i18n.js`.

## Языки

- Сейчас: `ru` (default), `en`.
- Новый язык: код в `supportedLocales` + полный блок ключей + имя в `LOCALE_NATIVE_NAMES`.
- Переключение: `?lang=`, `localStorage` (`obratka.locale`), кнопка `data-action="lang"`.

## Жёсткое правило

Весь **видимый интерфейс** (кнопки, aria-label, title, модалки, `document.title`) — только через локали:

- разметка: `data-i18n`, `data-i18n-aria`, `data-i18n-title` + `applyDocumentI18n()`;
- динамика: `getStrings()` / `formatString()`;
- запрещён хардкод UI-строк без ключа (имена людей / URL портфолио — данные, не UI-copy).
- `formatPortfolioRole` — всегда EN Title Case (не UI-локаль); исключение без грейда — `gradeUndefined`.

## Префиксы ключей

| Префикс | Область |
|---------|---------|
| `metaTitle` / `metaTitleAttention` / `metaTitleDesktopOnly` | `document.title` / мигание вкладки при конце таймера / desktop-only гейт |
| `desktopOnly*` | оверлей «только с компьютера» (&lt;768px) |
| `referral*` | invite gate |
| `auth*` / `authCode*` / `authOtp*` / `authIdentityConflict` | регистрация / OTP |
| `home*` / `homeInvite*` / `homeNoSlots*` / `homeAlreadyReviewed*` / `homeCardReviewed*` / `homeReviewIntro*` / `homeMineNotReady*` / `homeFeedFilter*` / `homeMineFilter*` / `homeEmptyMineActive` / `homeEmptyMineCompleted` / `homeEmptyFeedReviewed` / `homeMineSlotFree*` / `homePendingLimit*` / `homeNotify*` / `notificationCloseAria` / `homeCardReport*` / `homeCardReportPending*` / `homeCardReviewer*` / `homeTabMineReadyAria` / `homeTabFeedNewAria` / `homeReputation*` / `homeBalance*` / `homeAccount*` / `homeContacts*` / `homeRulesCloseAria` / `homeFeedback*` / `homeLegendaryOnline*` / `homeRating*` | home + notices / intro / mine gate / Ждёт·Уже отревьюено / Ещё·Завершенные / free-slot / toast notify / seen-dot / слоты ревьюеров / репутация / уточки / invite-текст (`homeInviteMessage`) / меню профиля / контакты / правила (текст в `content/rules.json`) / FAB связи / «Топы в сети» / рейтинг топ-50 по репутации |
| `gradeUndefined` | подпись без известного `profiles.grade` (карточки / рейтинг / report / submit `portfolios.role`) |
| `modalCloseAria` | общая модалка (`app-modal`) |
| `onboarding*` | `/onboarding` (в т.ч. `onboardingSaveFailed`) |
| `url*` / `urlScreenBack*` | `/portfolio` (в т.ч. чип «На главную») |
| `ban*` | `/banned` |
| `frame*` / `controls*` / `embedBlocked*` (`embedBlockedStep*` · `embedBlockedOpen` · `embedBlockedOpenSite` · `embedBlockedAria` · `embedBlockedMediaAria`) | iframe-shell `/review` (external: видео-слот + шаги + CTA; после старта CTA → «Открыть сайт») |
| `reportScreen*` / `reportSheet*` / `reportComplaint*` / `complaintTag*` | `/report` + просмотр листа (side-panel) + жалоба на лист |
| `reviewRec*` / `reviewAdviceRec*` / `reportDictationTitle` / `reviewAbort*` | чип rec на `/review`, микрофон в поле совета, секция надиктовки в листе, confirm «Прервать ревью?» |
| `reviewTier*` / `reviewPain*` / `reviewGrade*` / `reviewStructure*` / `reviewMetrics*` | шаги квиза (tier = рыночный уровень, не `profiles.tier`) |
| `reportTier*` / `reportSummary*` / `reportCross*` / `reportPain*` / `reportGrade*` / `reportContext*` / `reportStructure*` / `reportMetrics*` / `reportVisual*` / `reportAdviceTitle` | тексты PDF-листа |
| `reviewContextShort` / `reviewVisualShort` / `reviewContextValue*` / `reviewVisualValue*` / `reviewContextHint*` / `reviewVisualHint*` | idle-заголовок шкал + ступени + приписки 1–5 ([`scale-slider`](../../src/components/scale-slider/README.md)) |
| `settings*` | `/settings` (side-panel профиля view-only: имя / Telegram / email / специальность / грейд / workplace; дата в description) |

## Добавление ключа

1. Ключ в `ru` и `en` (и во всех `supportedLocales`).
2. Разметка или `formatString(t.key, vars)`.
3. Не оставлять язык «наполовину» переведённым.

```html
<button data-i18n="notesButton" data-i18n-title="notesButtonTitle">Заметки</button>
```
