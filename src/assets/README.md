# `src/assets/` — ассеты для импорта из кода

В этой папке лежат файлы, которые подключаются через `import` в JS-модулях.

## Состав

- `brand/logo.svg` — брендовый логотип, используется в компоненте логотипа.
