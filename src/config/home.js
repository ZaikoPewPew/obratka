/**
 * Вкладка «Рейтинг» (топ-50) на `/home`.
 * Учёт `profiles.reputation` / жалобы / settle / бан от флага не зависят.
 * Включить → `true` + redeploy Pages.
 */
export const RATING_TAB_ENABLED = false;
